import os

while True:
    # TODO: Check for new files (created)
    # TODO: If has new files save this information (json, mongo)
    # TODO: Upload new files
    # TODO: Check if all files are consistents (updated, deleted)
    time.sleep(60) # Next iteration will be after 1 minute
